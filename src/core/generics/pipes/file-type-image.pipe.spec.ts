import { FileTypeImagePipe } from './file-type-image.pipe';

describe('DocumentTypeImagePipe', () => {
  it('create an instance', () => {
    const pipe = new FileTypeImagePipe();
    expect(pipe).toBeTruthy();
  });
});
