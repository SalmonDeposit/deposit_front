import { DocumentTypeImagePipePipe } from './document-type-image-pipe.pipe';

describe('DocumentTypeImagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentTypeImagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
